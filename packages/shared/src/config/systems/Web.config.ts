import { ConfigSystem as System } from "../ConfigSystem";
import { ConfigParam as Param } from "../ConfigParam";

export const WebConfig = new System({ enabled: true, description: "Config settings for website, makes entire site inaccessible if disabled" }, {

})